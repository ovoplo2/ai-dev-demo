# Project rules

- Work only inside this repository.
- Keep changes minimal and easy to review.
- After each meaningful code change, run the relevant checks again.
- If a check fails, fix the issue and re-run until it passes.
- Do not touch deployment config, secrets, payment, auth, or external credentials.
- Always summarize changed files, commands run, and remaining risks.
